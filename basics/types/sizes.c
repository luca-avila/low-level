#include <stdio.h>

int main(void) {

    printf("Int size: %zu\n", sizeof(int));
    printf("Char size: %zu\n", sizeof(char));
    printf("Double size: %zu\n", sizeof(double));
    printf("Float size: %zu\n", sizeof(float));
    return 0;
}